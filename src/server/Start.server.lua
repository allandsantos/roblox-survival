local sharedStorage = game:GetService("ReplicatedStorage").Shared
local PlayerModule = require(sharedStorage.PlayerModule)

-- local localPlayer = PlayerModule.GetLocalPlayer()
-- print(PlayerModule.HungerGet(localPlayer))

-- PlayerModule.HungerSet(localPlayer, 50)

-- print(PlayerModule.HungerGet(localPlayer))
